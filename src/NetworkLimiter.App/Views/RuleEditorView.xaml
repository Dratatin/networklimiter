<UserControl x:Class="NetworkLimiter.App.Views.RuleEditorView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:vm="clr-namespace:NetworkLimiter.App.ViewModels"
             d:DataContext="{d:DesignInstance Type=vm:RuleEditorSessionViewModel}"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d">

    <!--
        Éditeur d'une règle. Le point exigé par FR-007 est que l'unité soit TOUJOURS visible et
        que les bornes soient rappelées pendant la frappe : un plafond saisi en croyant parler
        de mégabits vaut huit fois ce que l'utilisateur pense.
    -->
    <Border Background="{DynamicResource {x:Static SystemColors.WindowBrushKey}}"
            BorderBrush="{DynamicResource {x:Static SystemColors.ActiveBorderBrushKey}}"
            BorderThickness="1"
            Padding="16">
        <StackPanel MinWidth="380">

            <TextBlock Text="{Binding Title}" FontSize="16" FontWeight="SemiBold" />

            <TextBlock Text="{Binding Target.ExecutablePath}"
                       Opacity="0.7"
                       TextWrapping="Wrap"
                       Margin="0,4,0,12" />

            <!-- Descente -->
            <TextBlock Text="Plafond en descente" FontWeight="SemiBold" />
            <ContentControl Content="{Binding Download}" ContentTemplate="{StaticResource RateInputTemplate}" />

            <!-- Montée -->
            <TextBlock Text="Plafond en montée" FontWeight="SemiBold" Margin="0,12,0,0" />
            <ContentControl Content="{Binding Upload}" ContentTemplate="{StaticResource RateInputTemplate}" />

            <CheckBox Content="Règle active"
                      IsChecked="{Binding Enabled}"
                      Margin="0,14,0,0" />

            <!--
                Le message de blocage reste visible pendant toute la saisie, et n'apparaît pas
                seulement après un clic sur Enregistrer : c'est la différence entre corriger une
                valeur et découvrir qu'on a perdu sa saisie.
            -->
            <TextBlock Text="{Binding BlockingMessage}"
                       Foreground="#B00020"
                       TextWrapping="Wrap"
                       Margin="0,10,0,0">
                <TextBlock.Style>
                    <Style TargetType="TextBlock">
                        <Setter Property="Visibility" Value="Visible" />
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding BlockingMessage}" Value="{x:Null}">
                                <Setter Property="Visibility" Value="Collapsed" />
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </TextBlock.Style>
            </TextBlock>

            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,16,0,0">
                <Button Content="Annuler"
                        MinWidth="90"
                        Margin="0,0,8,0"
                        Command="{Binding DataContext.CancelEditCommand,
                                          RelativeSource={RelativeSource AncestorType=Window}}" />

                <Button Content="Enregistrer"
                        MinWidth="110"
                        IsDefault="True"
                        IsEnabled="{Binding CanSave}"
                        Command="{Binding DataContext.SaveRuleCommand,
                                          RelativeSource={RelativeSource AncestorType=Window}}" />
            </StackPanel>
        </StackPanel>
    </Border>
</UserControl>
