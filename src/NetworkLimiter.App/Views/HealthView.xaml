<UserControl x:Class="NetworkLimiter.App.Views.HealthView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:vm="clr-namespace:NetworkLimiter.App.ViewModels"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             d:DataContext="{d:DesignInstance Type=vm:HealthViewModel}"
             mc:Ignorable="d">

    <!--
        Panneau d'état de santé (T118, FR-026). Chaque constat porte son action : afficher
        « pilote non chargé » informe sans aider, et laisse devant une impasse quelqu'un qui
        n'a pas à connaître WinDivert.
    -->
    <UserControl.Resources>
        <Style x:Key="FindingBorder" TargetType="Border">
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="Padding" Value="10,8" />
            <Setter Property="Margin" Value="0,0,0,6" />
            <Setter Property="Background" Value="#F1F8F1" />
            <Setter Property="BorderBrush" Value="#C6E0C6" />
            <Style.Triggers>
                <DataTrigger Binding="{Binding Severity}" Value="Warning">
                    <Setter Property="Background" Value="#FFF4E5" />
                    <Setter Property="BorderBrush" Value="#E8C27A" />
                </DataTrigger>
                <DataTrigger Binding="{Binding Severity}" Value="Blocking">
                    <Setter Property="Background" Value="#FDECEC" />
                    <Setter Property="BorderBrush" Value="#E0A5A5" />
                </DataTrigger>
            </Style.Triggers>
        </Style>
    </UserControl.Resources>

    <DockPanel>
        <TextBlock DockPanel.Dock="Top"
                   Text="État du service"
                   FontWeight="SemiBold"
                   Margin="0,0,0,6" />

        <ItemsControl ItemsSource="{Binding Findings}">
            <ItemsControl.ItemTemplate>
                <DataTemplate DataType="{x:Type vm:HealthFinding}">
                    <Border Style="{StaticResource FindingBorder}">
                        <StackPanel>
                            <TextBlock Text="{Binding Title}"
                                       TextWrapping="Wrap"
                                       FontWeight="SemiBold" />

                            <TextBlock Text="{Binding Action}"
                                       TextWrapping="Wrap"
                                       Opacity="0.8"
                                       Margin="0,3,0,0"
                                       Visibility="{Binding Action, Converter={StaticResource TextToVisibility}}" />
                        </StackPanel>
                    </Border>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>
    </DockPanel>
</UserControl>
